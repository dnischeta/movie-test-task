import type { Meta } from '@storybook/vue3'
import AppFooter from './AppFooter.vue'

const meta: Meta = {
  title: 'App/Footer',
  component: AppFooter,
}

export default meta

export const Default = {}
