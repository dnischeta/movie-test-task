import type { Meta } from '@storybook/vue3'
import AppHeader from './AppHeader.vue'

const meta: Meta = {
  title: 'App/Header',
  component: AppHeader,
}

export default meta

export const Default = {}
